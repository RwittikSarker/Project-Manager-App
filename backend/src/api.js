const express = require("express");
const router = express.Router();
const PORT = 1634;

// router.listen(
//     PORT,
//     () => console.log(`working on http://localhost:${PORT}`)
// )

router.get()